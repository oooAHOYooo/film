# Summer CLI

This folder holds the command-line entrypoint for the Summer production tools.

Run it from `pages/summer` with:

```bash
python cli/film.py <command>
```

`pages/summer/film.py` stays as a compatibility wrapper for older paths.
